{
  "name": "github-metrics-wrapper",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "lint": "node --check api/github/[...path].js && node --check api/webhooks/github.js && node --check src/github-wrapper.js",
    "deploy": "vercel --prod"
  }
}
